// Your task is to reimplement a simplified version of the function reduce.

// reduce executes a reducer function (that you provide) on each element of
// the array, resulting in a single output value.

// As input, reduce takes two parameters.

// An array
// A function
// As output, reduce returns the single value resulting from the function's reduction.
// DO NOT use array.reduce

// Note that your function parameter takes two parameters.

// To help you implement reduce, observe the examples below.

// let accum = 0;
// reduce([1,2,3,4],function(accumulator,currentVal){
//   return accumulator+currentVal;
// }, accum) //=> 10

// reduce(['a','b','c','d'],function(accumulator,currentVal){
//   return accumulator+currentVal;
// }) //=> 'abcd'

function reduce(array, func) {
  // Your work here!
}
